import React, { Component } from "react"

class Resume extends Component {
  render () {
    return (
      <div>
        <p>Nothing here yet</p>
        {/* Check if :resumeId equals to an existing resume */}
      </div>
    )
  }
}

export default Resume
